<body>
    <nav class="navbar">
    <a href="{{ path('home') }}" class="logplus">Logo</a>
        <div class="navbar-container">
            <a href="{{ path('home') }}" class="navbar-brand">
            <img src="{{ asset('img/logo.png') }}" alt="Logo">
            </a>
                <div class="nav-links"> 
                    <ul class="navlist">
                        <li><a href="{{ path('realisations') }}">Réalisation</a></li>
                        <li><a href="{{ path('actualites') }}">Actualités</a></li>
                        <li class="portfolio">
                            <a href="#">Portfolio</a>
                            <ul class="submenu">
                                
                                {% for categorie in categorieNavbar() %}
                                    <li>
                                        <a href="{{ path('portfolio_categorie', {'slug': categorie.slug}) }}">
                                            {{ categorie.nom }} 
                                        </a>
                                    </li>
                                {% endfor %}
                                
                            </ul>
                        </li>
                        <li><a href="{{ path('a_propos') }}">A propos</a></li>
                        <li><a href="{{ path('contact') }}" class="nav-button">Me contacter</a></li>
                    </ul>
                </div>
             
                <img src="{{ asset('img/menuburger.png') }}" alt="menuburger" class="menuburger">
        </div>
    </nav>
    <header class="nav-header"></header>
<script src="{{asset('js/main.js')}}"></script>
</body>
</html>



